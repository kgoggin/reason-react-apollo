{
  "name": "reason-react-apollo",
  "version": "1.2.0-alpha.4",
  "dependencies": {
    "reason-future": "^2.5.0"
  },
  "devDependencies": {
    "bs-platform": "^5.0.6",
    "reason-react": "^0.7.1"
  },
  "peerDependencies": {
    "@apollo/react-hooks": "^3.0.0",
    "apollo-boost": "0.4.4",
    "bs-platform": "^7.2.2",
    "reason-react": "^0.7.1"
  },
  "scripts": {
    "bsb:watch": "bsb -clean-world -make-world -w",
    "bsb:build": "bsb -clean-world -make-world",
    "build": "yarn run bsb:build"
  }
}
